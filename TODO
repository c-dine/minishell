- heredoc
- exit status
    130: ctrl+c dans cmd en cours (cat, heredoc)
    131: ctrld+d dans cmd inf (?? pas sur)
- quand compilation d'un fichier qui segfault -> recuperer signal segfault
- lors de l'echec d'execve (= execute un fichier existant non executable), pour quitter
    il faut exit autant de fois que d'execve rates
- couleurs bugs
- ouvre sous shell quand met slash /

Leaks ? :
- fuites memoire quand exit du process enfant quand probleme de fichier -> CMD : < lolilolol cat
- leaks getcwd

cdine:
- ulimit -n 5 => boucle a l'infini qund 1 piep alors aue devrai pas + qund la limite est atteinte quitte minishell devrait redonne un prompt
- 	echo $"HOME" -> echo HOME" -> quote not closed			ok
-	ne repere pas le heredoc place avant la commande		ok
- dans builtin in -> check existance des mots apres commande

ntan
-	heredoc se lance sans delim								ok
- 	print error avec ctrl D									ok
-	heredoc double quote 									(y a moyen c bon)
-	heredoc expands tester $"HOME" "$HOME"					ca marche ? mais bug sur $"HOME"
- 	signaux faire des trucs avec l'entree standard
-	echo salut > "truc"										(askiparait c bon)
- 	def signaux
-	ft_quotes laisse des espaces a la fin parfois			-> fix pour heredoc










Mini erreurs :
- echo oui | echo non | echo hola | grep oui
- ifconfig|grep :