declaration vriable globale ?

cdine:
- export var="cat Makefile | grep >"
- quand chevron dans variable, ne peut pas afficher car syntax error -> remplacer variables apres parsing et avant retrait quotes ?

ntan
- fct exit